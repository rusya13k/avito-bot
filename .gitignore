# H3: секреты и пользовательские данные не уходят в git
.env
.env.local
.env.*.local

# config.json теперь без секретов (плейсхолдеры). Реальные значения
# подкладываются через .env (apply_env_overrides). Поэтому коммитим.

# Куки и логин-данные аккаунтов
cookies.json
accounts/
# G2: per-account конфиг (содержит phone/password/adspower_id —
# по факту секрет, не должен уезжать в git)
accounts.json

# Список прокси (host:port:user:pass — секрет, не коммитим)
proxies.txt

# SQLite база
*.db
*.db-journal
*.db-wal
*.db-shm

# Логи
*.log
logs/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# venv
.venv/
venv/
env/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# AI tooling (локальные кэши, аналог .vscode/.idea)
.claude/
.omc/

# Устаревший анализ кода (описывает архитектуру до Sprint 1-5/G1/G2)
codebase_analysis.md

# Страховочный патч от ruff format — временный, можно удалить вручную
.ruff-format.undo.patch

# Случайный мусор на диске (Windows reserved name + пустой шаблонный файл)
nul
Новый текстовый документ.txt
